# RoachRace UI Package

Custom UI package for RoachRace game.

## Dependencies

- Unity UI (UGUI)
- TextMesh Pro
- Input System

## Installation

This package is already installed as a local package in the project.

## Usage

Add your UI scripts to the `Runtime` folder. All scripts will be part of the `RoachRace.UI` namespace.
